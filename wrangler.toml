name = "train-crecc"
compatibility_date = "2024-09-23"
pages_build_output_dir = "./frontend"

[build]
command = ""

# Cloudflare Pages auto-detects the functions/ directory at the project root.
# The /api/reach endpoint is defined in functions/api/reach.js and reads
# frontend/data/reach.json at build time.
